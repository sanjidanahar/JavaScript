const first = "Sanjida";
const last =  "Nahar";

const fullName = first + ' ' + last;
// console.log(fullName);
const fullName2 = first.concat(' ').concat(last);  //concat akoi line a onkbar use hoy, ata diye add kora hoy
console.log(fullName2);


// include      include true / false dekhay
console.log(last.includes('x')); 
